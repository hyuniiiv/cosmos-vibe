# Spawn command

Replace this file with the **exact** command you ran. Don't paraphrase — the verbatim invocation is what makes this reproducible.

```
/cosmos:spawn --goal "<your goal>" --strategies "<strategy1>,<strategy2>,<strategy3>"
```

Optional: paste the immediate output `/cosmos:spawn` printed (the "🌌 Spawning N cosmos..." block with worktree paths). That confirms cosmos count, names, and strategy mapping.
